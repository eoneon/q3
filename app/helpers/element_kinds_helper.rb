module ElementKindsHelper
  # def exclusive_vl(parent_obj, child_obj, other_obj)
  #   #obj_set = parent_obj.public_send(kollection_name(child_obj)).pluck(:name).uniq
  #   set = []
  #   set = parent_obj.public_send(kollection_name(child_obj)).map{|obj| obj.public_send(kollection_name(other_obj)).first}
  #   #konstant(child_obj).where.not(name: obj_set)
  #   konstant(other_obj).where.not(id: set)
  # end
end
