<div class="input-group input-group-sm align-items-center bg-disabled">
  <button class="btn btn-sm rounded-circle bg-transparent toggle-input-access" type="button"><%= (fa_icon "toggle-off") %></button>
  <% if dom_cond(form_id, 'category') %>
    <%= f.grouped_collection_select :element_id, filtered_thru_vl(poly_element, 'element', 'element_kind'), :elements, :name, :id, :name, {prompt: "- select element name -"}, class: "form-control border-0 ml-2", disabled: true %>
  <% else %>
    <%= f.collection_select :element_id, filtered_vl(poly_element, 'element'), :id, :name, {prompt: "- select element name -"}, class: "form-control border-0 ml-2", disabled: true %>
  <% end %>
  <button class="btn btn-sm bg-transparent rounded-circle text-primary" type="submit" disabled><%= (fa_icon "plus-circle") %></button>
</div>
