<% if poly_element.present? && poly_element.element_joins.any? %>
  <% poly_element.sorted_element_joins.each do |element_join| %>
    <%= render "elements/poly_elements/show", poly_element: poly_element, element: element_join.element %>
  <% end %>
<% end %>
