<% if element_kind.present? && element_kind.elements.any? %>
  <% element_kind.sorted_elements.each do |element| %>
    <%= render "elements/show", element_group: element_group, element: element %>
  <% end %>
<% end %>
