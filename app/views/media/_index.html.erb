<% if origin.present? && origin.item_groups.any? && origin.sti_item_groups(target).any? %>
  <% origin.sti_item_groups(target).each do |item_group| %>
    <%= render "media/show", origin: origin, target: item_group.target, item_group: item_group  %>
  <% end %>
<% end %>
