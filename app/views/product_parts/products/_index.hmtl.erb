<% if origin_sti_item_groups = origin_sti_item_groups?(origin, type) %>
  <% origin_sti_item_groups.each do |item_group| %>
    <%#= render "sub_parts/show", origin: origin, target: item_group.target, item_group: item_group, obj_ref: dom_ref(origin, item_group.target, item_group) %>
  <% end %>
<% end %>
