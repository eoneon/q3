<% product.sorted_groups.each do |item_group| %>
  <%= render "product_parts/products/show", product: product, product_part: item_group.target, item_group: item_group, obj_ref: dom_ref(product, item_group.target, item_group) %>
<% end %>
