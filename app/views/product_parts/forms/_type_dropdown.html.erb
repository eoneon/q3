<div class="input-group"><!-- mb-3 -->
  <div class="input-group-prepend">
    <button class="btn btn-outline-secondary dropdown-toggle type-btn" type="button" data-toggle="dropdown" aria-expanded="false">choose type</button>
    <div class="dropdown-menu">
      <a class="dropdown-item select-opt active" href="#" data-name="choose type" data-value="">-choose type-</a>
      <% sti_abbrv_vl(:product_part).each do |opt| %>
        <a class="dropdown-item select-opt" href="#" data-name="<%= opt[0] %>" data-value="<%= opt[-1] %>"><%= opt[0] %></a>
      <% end %>
    </div>
  </div>
</div>
