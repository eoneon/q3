<% if elementable.present? && elementable.element_groups.any? %>
  <% elementable.sorted_element_groups.each do |element_group| %>
    <%= render "element_kinds/poly_fields/show", elementable: elementable, element_group: element_group, element_kind: element_group.element_kind %>
  <% end %>
<% end %>
