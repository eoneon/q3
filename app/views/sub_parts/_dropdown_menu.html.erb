<div class="dropdown-menu">
  <a class="dropdown-item disabled" data-toggle="collapse" href='#<%= dom_ref(origin, target, "edit") %>'>Edit <%= obj_to_cap_hyph_str(target) %></a>
  <div class="dropdown-divider"></div>
  <a class="dropdown-item" data-toggle="collapse" href='#<%= dom_ref(target, "#{to_snake(target.sti_field_type)}-item_group-add") %>'>Add <%= obj_to_cap_hyph_str(target.sti_field_type) %></a>
  <div class="dropdown-divider"></div>
  <% self_join_assocs(target, :product_kind).each do |sub_target| %>
    <a class="dropdown-item" data-toggle="collapse" href='#<%= dom_ref(target, "#{sub_target}-item_group-add") %>'>Add <%= obj_to_cap_hyph_str(sub_target) %></a>
  <% end %>
</div>
