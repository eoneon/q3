$("#category-show").html("<%= j render 'categories/show', category: @category %>");
configCategory("#category-edit");

function configCategory(target){
  configShow(target);
  getDeleteBtn(target).attr("href", "<%= category_path(@category) %>");
}

//unecessary: deletion just returns category to defaults
// function deconfigShow(show) {
//   var form = $(show).find("form");
//   disableToggleInput(form);
//   getCaret(form).prop("disabled", true).find("i").addClass("fa-caret-right").removeClass("fa-caret-down");
//   getLabel(form).toggleClass("text-light text-muted");
//   getDropdownBtn(form).addClass("disabled").removeClass("text-info").attr("href", "");
//   getDeleteBtn(form).addClass("disabled").attr("href", "");
//   disableInputs(form);
// }
//
// function disableInputs(form) {
//   getInputs(form).prop("disabled", true).addClass("disabled");
// }
