<div class="media">
  <div class="media-body">
    <tr>
    <td><%= category.id %></td>
    <td><%= link_to category.name, category_path(category) %></td>
    <td><%= link_to 'Edit', edit_category_path(category), class: "btn btn-primary btn-xs" %> <%= link_to 'Delete', category,  method: :delete, class: 'btn btn-danger btn-xs', data: {confirm: "Are you sure?"} %></td>
    </tr>
 </div>
</div>
