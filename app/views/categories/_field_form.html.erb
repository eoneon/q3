<%= form_for [category, FieldGroup.new] do |f| %>
  <%= f.collection_select :field_id, Field.all, :id, :name, {class: 'form-control', include_blank: true} %>
  <%= f.submit "Add category field" %>
<% end %>
