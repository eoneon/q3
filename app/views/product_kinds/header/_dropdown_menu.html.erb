<div class="dropdown-menu">
  <a class="dropdown-item disabled" data-toggle="collapse" href='#<%= dom_ref(origin, "edit") %>'>Edit <%= obj_to_cap_hyph_str(origin) %></a>
  <% self_join_assocs(origin).each do |target| %>
    <a class="dropdown-item" data-toggle="collapse" href='#<%= dom_ref(origin, "#{target}-item_group-add") %>'>Add <%= obj_to_cap_hyph_str(target) %></a>
  <% end %>
</div>
