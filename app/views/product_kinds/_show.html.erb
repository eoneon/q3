<div class="card border m-1 border-dark <%= dom_ref(origin, 'card') %>" id="<%= dom_ref(origin, 'show') %>">
  <div class="card-header <%= dom_ref(origin, 'header') %> pt-0">
    <%= render "product_kinds/header/header", origin: origin %>
  </div>

  <div class="card-body p-2 collapse show" id="<%= dom_ref(origin, 'body') %>">
    <% self_join_assocs(origin).each do |target| %>
      <div id='<%= dom_ref(origin, "#{to_snake(target)}-index") %>'>
        <%= render "media/index", origin: origin, target: target %>
      </div>
    <% end %>
  </div>
</div>
