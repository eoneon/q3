<%= f.fields_for :properties, OpenStruct.new(item_type.properties) do |ff| %>
  <% item_type.category.fields.each do |field| %>
    <%= render "item_types/fields/#{field.field_type}", item_type: item_type, field: field, ff: ff %>
  <% end %>
<% end %>
