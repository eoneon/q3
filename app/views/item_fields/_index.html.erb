<% if origin.present? && origin.grouped_subklass('ProductKindField').any? %>
  <% origin.grouped_subklass(origin.field_subklass).each do |item_group| %>
    <%= render "item_fields/show", origin: origin, target: item_group.target, item_group: item_group  %>
  <% end %>
<% end %>
