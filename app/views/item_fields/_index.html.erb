<% if origin.present? && origin.sti_item_groups(origin.sti_field_type).any? %>
  <% origin.sti_item_groups(origin.sti_field_type).each do |item_group| %>
    <%= render "item_fields/show", origin: origin, target: item_group.target, item_group: item_group  %>
  <% end %>
<% end %>
