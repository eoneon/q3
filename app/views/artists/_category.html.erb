<div class="media">
  <div class="media-body">
    <tr>
    <td><%= artist.id %></td>
    <td><%= link_to artist.name, artist_path(artist) %></td>
    <td><%= link_to 'Edit', edit_artist_path(artist), class: "btn btn-primary btn-xs" %> <%= link_to 'Delete', artist,  method: :delete, class: 'btn btn-danger btn-xs', data: {confirm: "Are you sure?"} %></td>
    </tr>
 </div>
</div>
