<div class="col-1">
  <%= f.collection_select to_fk(sub_part.name), sub_part, :id, :name, {prompt: "- select #{obj_to_hyph_str(sub_part.name)} -"}, class: "form-control form-control-sm border-0"  %>
  <%#= sub_part_select(obj, f: f) %>
</div>
